<Window x:Class="RevitAPITraningUI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITraningUI"
             mc:Ignorable="d" 
             Height="250" Width="450"
        Title="Выберите кнопку">
    <StackPanel>
        <Label Content="количество труб" Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectCommand}" Margin="5"/>

        <Label Content="объём всех стен" Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectCommand1}" Margin="5"/>

        <Label Content="количество дверей" Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectCommand2}" Margin="5"/>
    </StackPanel>
</Window>
